{
  "entities": {
    "ChatMessage": {
      "title": "Chat Message",
      "description": "Represents a message sent in the global cricket chat.",
      "type": "object",
      "properties": {
        "user": { "type": "string", "description": "The handle of the message sender." },
        "text": { "type": "string", "description": "The content of the message." },
        "timestamp": { "type": "string", "format": "date-time", "description": "When the message was sent." },
        "isAi": { "type": "boolean", "description": "Whether the message was generated by AI." }
      },
      "required": ["user", "text", "timestamp"]
    }
  },
  "firestore": {
    "messages": {
      "schema": "ChatMessage",
      "description": "Global collection of chat messages for real-time interaction."
    }
  }
}
