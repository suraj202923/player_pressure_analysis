runtime: nodejs20
instance_class: F2

handlers:
  - url: /api/.*
    script: auto
  - url: /socket.io
    script: auto
  - url: /(.*)
    static_files: dist/\1
    upload: dist/.*
    secure: always

env_variables:
  NODE_ENV: production
